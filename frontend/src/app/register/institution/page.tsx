import InstitutionRegisterScreen from '@/screens/Institution/InstitutionRegisterScreen';

export const metadata = {
    title: 'Institution Registration - MERMS',
    description: 'Register your healthcare facility on the Medical Emergency Response Management System.',
};

export default function InstitutionRegisterPage() {
    return <InstitutionRegisterScreen />;
}
